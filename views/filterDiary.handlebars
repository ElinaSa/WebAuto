<label for="registerNumber">Valitse rekisterinumero</label>
<select name="rekisterinumero" id="rekisterinumero">
    {{#each registers}}
    <option value="{{rekisterinumero}}">{{rekisterinumero}}</option>
    {{/each}}
</select>
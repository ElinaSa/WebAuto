<label for="registerNumber">Valitse rekisterinumero</label><br>
<select class="input-field" name="rekisterinumero" id="rekisterinumero">
    {{#each registers}}
        <option value="{{rekisterinumero}}">{{rekisterinumero}}</option>
    {{/each}}
</select>